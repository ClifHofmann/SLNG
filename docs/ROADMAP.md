# Roadmap

This roadmap is structured into **MVP Milestones** focused on progressive end-user capabilities (playable increments), supported by **AI-sized tasks** with clear specs and acceptance criteria.

## How we work

Each task follows a **spec → test → implement → review** loop:

1. The owning agent reads the task and writes/extends a short spec if needed.
2. For protocol and asset work, write the test first (output must be verifiable).
3. Implement against the stable interface of the layer.
4. `code-reviewer` checks it against `AGENTS.md`, then it merges to `main`.

### Workstreams (parallel tracks)

| Track | Area | Lives in |
|---|---|---|
| `net` | Protocol, login, regions | `src/SLNG.Net` |
| `core` | World model / ECS | `src/SLNG.Core` |
| `assets` | Decode, mesh, materials, cache | `src/SLNG.Assets` |
| `render` | Godot scene, lighting, camera | `app/` |
| `ui` | HUD, chat, login screen | `app/` |
| `infra` | Build, CI, packaging, installer | root / `tools/` |

---

## MVP 1 — Foundation & Playable Client ✅

**Goal:** Connect to a grid, render terrain/prims/PBR/lighting, walk around, chat, and render Bento avatar.

### Phase 1A: Connection & World Engine (M0 – M2)
- [x] **M0-1** Scaffold solution & Godot app ✅
- [x] **M0-2** Login flow via LibreMetaverse ✅
- [x] **M0-3** Event logger ✅
- [x] **M0-4** Godot boot scene ✅
- [x] **M0-5** Test harness & OpenSim local grid ✅
- [x] **M1-1** World model / ECS ✅
- [x] **M1-2** Terrain rendering & collision ✅
- [x] **M1-3** Primitive shapes ✅
- [x] **M1-4** Free-fly camera & scene sync ✅
- [x] **M1-5** Interest management ✅
- [x] **M2-1** Mesh fetch & LOD selection ✅
- [x] **M2-2** JPEG2000 decode pool ✅
- [x] **M2-3** PBR material resolve ✅
- [x] **M2-4** Texture streaming & VRAM budget ✅
- [x] **M2-5** Lighting, CSM shadows & post-fx ✅
- [x] **M2-6** Terrain textures & water plane ✅

### Phase 1B: Controls & Bento Avatar (M3 – M4)
- [x] **M3-1** Avatar movement & network sync ✅
- [x] **M3-2** Local chat UI ✅
- [x] **M3-3** Third/first-person camera controller ✅
- [x] **M3-4** Own avatar render ✅
- [x] **M4-1** Bento skeleton & base mesh ✅
- [x] **M4-2** Animation decode & playback ✅
- [x] **M4-3** Appearance & Bakes-on-Mesh (BoM) ✅
- [x] **M4-4** Mesh attachments ✅
- [x] **M4-5** Vertex morphs engine pipeline ✅
- [x] **M4-6** Morphed body rendering ✅
- [x] **M4-8** SL-faithful joint composition ✅
- [x] **M4-9** HUD attachments overlay ✅

---

## MVP 1.5 — Build Distribution & Client Config ✅

**Goal:** Automated CI/CD pipeline, standalone Windows Installer for test user distribution, and app configuration persistence.

| ID | Task | Track | Owner | Agent | Tool | Dep | Done when |
|---|---|---|---|---|---|---|---|
| M1.5-1 | Godot Headless Export Script ✅ | infra | | DevOps | claude | M0-1 | Headless Windows export (.exe + .pck + .NET assemblies) runs in CI. **Acceptance deviated deliberately:** there is no standalone PowerShell script — the export is the "Export Godot App" step of `.github/workflows/build_installer.yml`, invoking `godot --headless --path app --export-release "Windows Desktop"` against the committed `app/export_presets.cfg`. A separate `.ps1` would have been a second copy of one command line, drifting from the one CI actually runs. |
| M1.5-2 | InnoSetup / Installer Spec & Script ✅ | infra | | DevOps | claude | M1.5-1 | `installer/PurisViewer.iss` bundles the export into a single-click installer; CI installs Inno Setup via Chocolatey and compiles it with the release tag passed in as `/DMyAppVersion`, so the installer's version always matches the release rather than the script's local-build fallback. |
| M1.5-3 | GitHub Actions Continuous Delivery ✅ | infra | | DevOps | claude | M1.5-2 | `.github/workflows/build_installer.yml` builds and publishes the installer for testers. **Acceptance deviated deliberately:** it triggers on `v*` **tags** (plus `workflow_dispatch`), not on every `main` push — a release per tag is what gives support an unambiguous "which build is this", which the v0.3.x round-trip debugging showed was the actual need. Attaches `Output/PurisViewer_Setup_*.exe` to the GitHub release via `softprops/action-gh-release`. |
| BUG-NET-01 | Camera-based Object Fetching (Zoom-Bug) ✅ | net | claude | protocol-re | claude | M1-5 | Objects do not load when zooming (Alt-Click) to a distant point. Interest management needs to account for camera position, not just avatar position. **Root cause:** `GridSession.SetMovement` only ever set the interest camera's *axes* (`Camera.LookDirection`), never `Camera.Position` — so LibreMetaverse's `AgentCamera` default `(128,128,20)` / `Far 128` went into every `AgentUpdate.CameraCenter`, and every sim built its interest list around region centre, not the avatar or the camera (`GridSession.cs:2520` was the only `src/` line touching the movement camera). **Fix on `fix/BUG-NET-01-camera-interest-fetch`:** `SetMovement` gained optional `cameraPosition` / `cameraForward` / `cameraFar` (`System.Numerics`, region-local — no LMV type crosses the boundary); it calls `Camera.LookAt` so `CameraCenter` follows the render camera. `AvatarController` feeds the finalised camera pose (via `RenderConfig.FromGodot` + the `(x,-z,y)` axis swap) into the existing 10 Hz send, with `Far = clamp(dist(cam,avatar)+DrawDistance, 128, 512)` so the avatar's surroundings stay loaded while zoomed. **Second half (`4fc727e`):** the network fix alone did nothing in-world — `ObjectRenderer`'s cull sweep still measured every object against the avatar only, so anything > DrawDistance from the avatar stayed hidden / VRAM-released regardless of the camera. Visibility + resource reload/release now run against `min(dist-to-avatar, dist-to-camera)` (`GetViewport().GetCamera3D()`); collision-shape repair stays avatar-only. **Confirmed in-world 2026-08-27**, merged to `main` at `v0.9.47-alpha`. [Spec](file:///E:/Git/SLNG/docs/specs/BUG-NET-01-camera-interest-fetch.md) |
| BUG-UI-02 | Camera Tools Tilt Inversion & Zoom Lockout ✅ | ui/render | claude | graphics-engineer | claude | M3-3 | User's "tilt" is the Camera-Controls **pan pad** (the square 4-way pad, "schieben"), not the round orbit pad — orbit was never broken. Pan left/right were inverted and the whole pad went dead once a focus/zoom target was active. **Fix on `fix/BUG-UI-02-03-camera-input`:** (1) `AvatarController` applied `_panOffset` only in the non-focused branch of the camera-target block, so any Alt-click focus (`_orbitTarget`) made the pad a no-op — moved `targetPos += Basis.X*panX + Basis.Y*panY` out of the `else` so it applies with a focus target too; (2) `CameraHUD` fed `pan_left` a `+X` step but `+X` is screen-right in the consumer — flipped left/right; (3) per-frame step `1.0` → `0.1` (`PanStep`); the old value was ~60 m/s held and threw the avatar off-screen on a tap. A first pass misread "tilt" as the orbit pad (swapped `rot_*`, eased `_panOffset` on rotate) and was reverted. Follow-up on user request: a **"Camera" tab in Preferences** (`CameraSettings` + `CameraPreferencesPage`, `preferences.cfg [camera]`) with live Orbit/Pan/Zoom speed multipliers (10–300%, default 100%) and a reset button — `CameraHUD` reads them per frame. Orbit base step lowered `0.05` → `0.035` (felt fast even at default) and the slider floor dropped `25%` → `10%` after in-world feedback. Builds, 322 tests green, format clean (solution; `app/` carries pre-existing whitespace debt). **Confirmed in-world 2026-08-27**, code-reviewed (no blockers), **merged to `main` (`d6a9765`)** at `v0.9.42-alpha`. [Spec](file:///E:/Git/SLNG/docs/specs/BUG-UI-02-camera-tilt.md) |
| BUG-UI-03 | Camera Input State on Focus Loss (Alt-Zoom) ✅ | ui | claude | ux-designer | claude | M3-3 | Zoom jumps if LMB is released outside the window because Godot misses the release event. Needs focus-out state reset. **Fix:** the Alt+LMB drag runs under `MouseMode.Hidden` (not Captured — RDP-safe, see the engage site), so a release outside the window is never delivered and the per-frame `Input.IsMouseButtonPressed` poll driving `_altOrbitActive` stays stuck until focus returns. New `AvatarController._Notification` aborts the drag (`_altOrbitActive=false`, restore `MouseMode.Visible`, clear the last-mouse-pos, set `_suppressOrbitUntilRelease` so it can't re-engage while the button is still held) on `WMWindowFocusOut` / `ApplicationFocusOut` / `WMMouseExit`. **Confirmed in-world 2026-08-27**, code-reviewed, **merged to `main` (`d6a9765`)**. [Spec](file:///E:/Git/SLNG/docs/specs/BUG-UI-03-camera-input-focus-loss.md) |
| FEAT-UI-03 | Boot Window Size & Profile Persistence ✅ | ui/infra | gemini | ux-designer | gemini | M0-4 | Client window resolution, grid profiles & user preferences persist across sessions in `user://logins.cfg`. [Spec](file:///E:/Git/SLNG/docs/specs/done/FEAT-UI-03-boot-window-persistence.md) |
| FEAT-UI-11 | Window State Persistence & Viewport Bounding ✅ | ui | claude | ux-designer | claude | FEAT-UI-03 | Save/restore size and position of floating UI windows. Clamp windows to visible screen bounds on resize to prevent off-screen loss. **Implemented on `feature/FEAT-UI-11-window-state-persistence`:** persistence already existed in `SLNGWindow` (`PersistId` → `preferences.cfg [window_geometry]`, save on drag/resize end, clamp on restore); this task adds (1) `SLNGWindow` subscribes to `GetViewport().SizeChanged` and re-clamps every window when the app window shrinks — `ClampToViewport` keeps ≥40 px of the title bar on screen, footprint `Size*Scale` so it's correct under UI scaling — persisting the corrected spot only if the window actually moved; unsubscribed in `_ExitTree`; (2) `PersistId` rolled out to `chat`, `inventory`, `create_landmark` (multi-instance windows — `ObjectEditWindow`/`ItemPropertiesWindow`/`ChatHistoryWindow` — deliberately excluded, they'd share one slot). Builds, 322 tests, format clean (solution; `app/` carries pre-existing whitespace debt). **Confirmed in-world 2026-08-27**, merged to `main` at `v0.9.46-alpha`. [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-UI-11-window-state-persistence.md) |
| FEAT-UI-12 | Camera Settings Persistence (FOV & Offset) ✅ | ui | claude | ux-designer | claude | FEAT-UI-03 | Save and restore advanced camera settings like field of view (FOV) and positional offset in user preferences. **Implemented on `feature/FEAT-UI-12-camera-settings-persistence`:** `CameraSettings` gained `Fov` (50–100°, def 75), `RearDistance` (1–20 m, def 4) and `FocusHeight` (0.5–3 m, def 1.8, replaces the hardcoded `1.8f` eye-height) in the same `preferences.cfg [camera]` section as the BUG-UI-02 speed sliders. A "View:" group in `CameraPreferencesPage` exposes all three; `AvatarController.SetCameraSettings` (wired from `Boot` post-login) applies FOV + distance at once, keeps FOV synced per frame so the slider is live, and uses `RearDistance` on every `ResetCamera` (Esc). i18n: en-US + de-DE. The "Rear view distance" slider needed a live re-apply (`4fc727e`) -- it only fed `_zoom` at login/Esc. Builds, 322 tests, format clean (solution; `app/` carries pre-existing whitespace debt). **Confirmed in-world 2026-08-27**, merged to `main` at `v0.9.47-alpha`. [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-UI-12-camera-settings-persistence.md) |
| FEAT-UI-07 | UI Scale Setting ✅ | ui | claude | ux-designer | claude | M5-3 | Preferences slider (80%–160%) scales every `SLNGWindow` live, persisted in `preferences.cfg`. [Spec](file:///E:/Git/SLNG/docs/specs/done/FEAT-UI-07-ui-scaling.md) |
| FEAT-UI-08 | Login / Boot Screen Rebrand (Puris Glassmorphism Theme) ✅ | ui | claude | ux-designer | claude | FEAT-UI-03 | Reskin login + loading screen to match user-provided "Puris Viewer" mockup (dark navy/teal glassmorphism, circular progress ring, step checklist). Implemented and merged to `main` (`bf102e2`). Visual testing and modal blur completed. [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-UI-08-login-rebrand.md) |
| FEAT-UI-10 | Settings UI Redesign & Footprint Reduction ✅ | ui | gemini | ux-designer | gemini | FEAT-UI-07 | Reduce the UI space usage by tightening margins. Keep Graphics and Photo tools unified in PreferencesWindow. Prepare ToolbarSettings for Firestorm-style multi-edge docking. **Landed on `main`**: tightened `SLNGWindow`/`TopMenu` header margins, smaller `PreferencesWindow` default footprint, and Graphics/Quality reintegrated as PreferencesWindow tabs (`2902cf8`, `6f28a1e`). `ToolbarSettings.ToolbarDockPosition` and multi-edge docking were implemented (`3f6b689`) and then deliberately reduced again to one permanently visible bottom-edge toolbar (`03d4042`) after the edge panels needed container-based layout rather than anchors (`7db7882`, `db99795`, `f27f51d`) — recorded in [SETTINGS_MAP.md](file:///E:/Git/SLNG/docs/SETTINGS_MAP.md). All acceptance criteria in the spec are met. [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-UI-10-settings-redesign.md) |
| FEAT-PERF-01 | Login-to-Usable Startup Latency ✅ | net/assets/render | gemini | performance-engineer | gemini | M0-2 | User-reported: client takes ~1 minute from successful login to a usable/interactive state (region load, terrain, nearby objects/avatars, own-avatar bake). Addressed via ThreadPool starvation fix (`SetMinThreads`) allowing parallel J2K decodes of cached assets. |
| FEAT-PERF-02 | Texture Loading Speed ✅ | assets/net/render | claude | asset-pipeline | gemini | M2-4 | Speed up how fast textures visually populate a scene. Phase 1: true single-flight fetch+decode dedup, centralize the GPU-upload path, and give sculpt maps their own fetch throttle. Phase 2: distance/screen-size-driven texture priority + local J2K downsampling (network discard reverted due to CoreJ2K fallback bugs). Concurrent fetch cap over HTTP was bumped from 4 to 32 (and 4 for sculpts) since the UDP truncation bug does not apply to TCP. [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-PERF-02-texture-loading-speed.md). |
| FEAT-NET-03 | LibreMetaverse 3.0.0 → 3.1.3 ✅ | net/assets | claude | protocol-re | claude | — | Upgrade the pinned LibreMetaverse packages (3.0.0, 23.06.2026) to 3.1.3 (24.07.2026) — 7 releases / 92 commits behind. Verified on `feature/FEAT-NET-03-libremetaverse-3.1.3`: solution + `app/` build with 0 errors, 133 tests green, headless boot clean, and both runtime-only reflection contracts still hold (`AssetManager.Texture` → `TexturePipeline.RequestTexture` with the same 7 positional params; `CoreJ2K.Skia`'s `SKBitmapImageCreator` still resolvable). Nothing in the diff touches UV/mesh generation (`MeshFoundry.cs` changed 3 lines, signatures only), so it does NOT address the open FEAT-RENDER-01 placement bug. Main theme is removing SkiaSharp from LMV's core via `ManagedImage`/`ITextureCodec`, which the release notes say "coincidentally fixes several alpha mask bugs" — worth checking against the avatar alpha work. Live-verified on OSGrid 2026-08-18: a full session loaded textures, meshes and avatars with no regression vs 3.0.0 — the acceptance criterion that could not be exercised headless. (Verified incidentally, while using the same session to run the FEAT-RENDER-01 texture placement rig, which needed exactly the texture/mesh path this upgrade put at risk.) |
| FEAT-INFRA-01 | Repo Hygiene & Headless Smoke Test ✅ | infra | claude | DevOps | claude | — | A cleanup pass with one real feature in it. **`--selftest` now exists** (`app/scripts/SelfTest.cs`): documented in AGENTS.md since the first commit, it had no handler at all and the command just sat at the login screen — which is why the FEAT-RENDER-04 shader changes were handed over unverified. It loads every `.gdshader`, every `res://i18n` locale (including a key-coverage diff against `en-US`) and the Bento skeleton through the engine, and exits non-zero on the first one that does not come up; no login, so it needs no credentials and no grid. The shader check reads the **uniform list**, because `ResourceLoader.Load` returns a Shader object for a file that did not parse while a rejected shader exposes no uniforms — verified by deliberately breaking `water.gdshader` and confirming FAIL + exit 1, rather than assuming the check can fire. `godot --headless --editor --quit` cannot do this: it reports success on a shader that cannot compile, which is how v0.7.36-alpha shipped a black sky dome. Wired into CI as a `selftest` job on `windows-latest` (deliberately not ubuntu: the .NET job is already red there for an unrelated native-dependency reason, and a second red X on the same workflow is indistinguishable from the first), together with `tools/check_shader_globals.py`, which existed but ran only when someone remembered. Also in the pass: `dotnet format` is clean at 0 violations — achieved by taking vendored `src/SLNG.Assets/PrimMesher/` (OpenSim, BSD) out of scope via `generated_code = true` instead of rewriting ~2300 lines of upstream code; 17 fully-merged local branches and 3 remote ones deleted, plus the leftover worktree registrations; `SLNG_Test_Sky.xml` moved from the repo root to `tools/testassets/eep-parity/`; and AGENTS.md now states that **`dotnet build SLNG.sln` compiles none of `app/`**, the trap behind more than one "fixed it but nothing changed" round. |

---

## MVP 2 — Navigation & Social Core ✅

**Goal:** Complete multi-user social interaction, navigation, and script dialogs.

| ID | Task | Track | Owner | Agent | Tool | Dep | Done when |
|---|---|---|---|---|---|---|---|
| M5-3 | Tabbed Chat, Friends & Groups Window ✅ | ui/net | claude | ux-designer | claude | M3-2 | Multi-tabbed window (Chat, Friends, Groups, Dynamic IMs) inheriting from `SLNGWindow`. Phases 1/1b/1c (shell, local chat migration, Friends, IM tabs) merged earlier. **Phase 2 landed** (`v0.9.61-alpha`): `GroupManager` wiring behind neutral DTOs (`GroupEntry`, `RequestGroups`/`GetGroups`/`GroupsUpdated` — no LMV type crosses the boundary), a `GroupsPanel` in the same two-pane shape as `FriendsPanel` (filter, initial badge standing in for the insignia, member title, `Group Chat` + `Mute chat` functional, `Profile`/`Leave` as tooltipped placeholders), full group chat (join/leave/send/receive) as its own conversation tab logging to `<Group>.txt`, and a per-group mute persisted in `preferences.cfg [group_mute]` that suppresses tab auto-open and unread badges (an already-open tab still receives — having the conversation in front of you outranks the mute). **The reason group chat had never appeared at all:** `OnInstantMessage` filtered on `Dialog != MessageFromAgent \|\| GroupIM`, but group chat arrives as **`SessionSend`** and its `GroupIM` flag is set only on a session's first message — both halves of that condition dropped it. LibreMetaverse's own `AgentManager.IsGroupMessage` (`GroupIM` **or** a known group-chat session) is the authoritative test and is what the handler uses now, instead of re-deriving the rule from dialog bytes; pinned by `GroupChatTests`. **Group invitations** (`v0.9.62-alpha`, user-reported "die Gruppeneinladung kam nicht an"): dialog 3 matched neither branch of `OnInstantMessage` and fell through silently. Now a `GroupInvitationWindow` prompt (server text, membership fee, Join/Decline) answering via `RespondToGroupInvitation`. Three source-verified details: LibreMetaverse's own `Groups.GroupInvitation` event is **unusable** — it fires synchronously and then replies from `args.Accept`, default `false`, so subscribing while asking the user would auto-decline everything (left unsubscribed, handled in our own `Self.IM`); the group id travels in `FromAgentID` and the reply is addressed to it with the invite's session id as transaction id (`llimprocessing.cpp:864`, `llviewermessage.cpp:681`); the fee is a network-order `S32` at the head of a 20-byte binary bucket (`llimprocessing.cpp:846-857`) — a wrong-size bucket reports fee 0 rather than dropping the invitation, which is what the viewer does. Deliberately still open: the chat-log path setting in Preferences (the default user-space path ships and works), group profile / leaving a group / the insignia texture, and the other unmodelled `Self.IM` dialogs (friendship offers, inventory offers, teleport requests, group notices) which are still dropped the same way this one was. Builds (solution + `app/`) + 363 tests + `dotnet format` + `--selftest` 26/26 (locale parity 186/186) green. **Confirmed in-world 2026-08-27:** group invitations (arrive, prompt, answer), the Groups list (memberships display) and group chat (send + receive). The per-group **mute toggle has not been separately exercised** yet. [Spec](file:///E:/Git/SLNG/docs/specs/M5-3-tabbed-chat-window.md) |

| BUG-NET-02 | Detach does nothing for a stale outfit link ✅ | net/ui | claude | protocol-re | claude | MVP3-1 | User-reported: an item shows as worn ("getragen") in the inventory but "Detach" silently does nothing, and Firestorm agrees the item is not attached. **Root cause:** `GetWornItemsMap` marks anything with a **Current-Outfit link** as worn, and `DetachAttachmentIntoInv` is matched server-side against **live attachments** — so for an item whose COF link survived without an actual attachment (crash, failed attach) the packet matches nothing and is a silent no-op. `DetachItemAsync` then removed the link from the **local** Store only, so the next folder fetch brought it straight back. The screenshot was the proof: the row read plain "(getragen)", not "(getragen an ‹Punkt›)" — the label the UI only writes when the item really is in `GetAttachmentsByItemId()`. **Fix:** `DetachItemAsync` returns `DetachResult(WasAttached, StaleLinksRemoved)`; when nothing was actually attached the matching COF links are moved **server-side to Trash** (Trash, not purge — the link is not the item, but an outfit is still user data). `InventoryPanel` now reports which of the two happened instead of always saying "Detached.". **Also added:** `DetachByLocalId` / `DetachAllAttachments(hudOnly)` (`ObjectDetach` by localId) plus World → "Detach All HUDs" / "Detach All Attachments", the escape hatch for an attachment inventory cannot address at all — it needs no AttachItemID. Detach feedback goes to `GD.Print` + a chat system line, not `LogMessage` (that writes to the boot LogPanel, which FEAT-UI-09 hides after login — the first version reported into an invisible control). 3 new tests. **Confirmed in-world 2026-08-27.** |
| FEAT-UI-13 | User Profile Window (Firestorm-style) ✅ | ui/net | claude | ux-designer | claude | M5-3 | Avatar profile window (2nd Life, 1st Life, Picks, Classifieds, private local Notes) with social actions (Add Friend, IM, Pay, Offer TP, Mute). **Implemented on `feature/FEAT-UI-13-user-profile-window`:** `UserProfileWindow : SLNGWindow`, one instance per avatar (keyed by agent id, like `ObjectEditWindow`). `GridSession` gained neutral profile DTOs + `AvatarPropertiesReceived`/`Interests`/`Groups`/`Picks`/`Classifieds` events (mapped off LibreMetaverse's `AvatarManager` replies, no LMV type crossing the boundary) and `OfferFriendship`/`OfferTeleport`/`PayAvatar`/`SetAvatarMuted`/`RequestMuteList` wrappers. Three entry points: right-click an avatar in-world (`ObjectSelectionController` avatar branch + `InWorldContextMenu.ShowAvatarMenu` — also fixes right-click-avatar wrongly showing the ground "Create" menu), the Friends tab's "Profile" button, and clicking a resident's `[url=avatar:…]` name in the chat log (`ChatMessageEvent` gained `SourceId`/`FromAgent`). Notes persist to `preferences.cfg [avatar_notes]`, never sent to the grid. Profile events buffered off the network thread + drained in `_Process` (DTOs aren't Variant-safe for `CallDeferred`). **Your own profile is editable** — About / 1st Life / web URL / search+mature flags / interests text, with a "Save Profile" button that writes `AvatarPropertiesUpdate` + `AvatarInterestsUpdate` (`GridSession.UpdateOwnProfile`/`UpdateOwnInterests`); social buttons + Notes tab omitted for self. **Picks show full detail** — snapshot, title, description, `SimName (x,y,z)` and a Teleport button (`RequestAvatarPickInfo` → `PickInfoReply` → `AvatarPickDetail`; `TeleportToGlobalPosition`). **Fully localised** — `ui.profile.*` (~58 keys, en-US + de-DE, `--selftest` parity 151/151). Deferred: pick "Show on Map" (needs MVP2-3) / pick editing / Classified detail, picture upload/pick, interest checkbox masks, clickable URL/partner. Also on this branch: `OnChatFromSimulator` now drops a truly-empty chat line (a worn radio object emitting `""` on channel 0), matching the Linden/Firestorm nearby-chat handler. Builds (solution + `app/`) + 340 tests + `--selftest` 24/24 green; `dotnet format` clean (solution; `app/` carries pre-existing whitespace debt). `v0.9.55-alpha`. **Confirmed in-world 2026-08-27** — own profile edited and saved, a Pick opened with its image/text/location and teleported to, another resident's profile opened from the in-world right-click menu. **2026-08-28 fix (proactive, found via MVP2-3's live-testing, not separately reproduced against this window):** `LoadTextureIntoAsync` never pinned its `GpuCache` result with `AddRef` — same bug class that crashed `WorldMapWindow.MapCanvas._Draw()` live that day (a disposed `Godot.ImageTexture`, since an unpinned entry is eviction-eligible the moment the shared 256 MB budget is exceeded by anything else). Fixed with a `RepinTexture(ref Guid pinnedId, Guid newId)` helper tracking which id is pinned per picture slot (profile pic, first-life pic, pick snapshot) so a slot's old id is released exactly when its new one is pinned, and all three release on `_ExitTree`. `v0.10.9-alpha`, full suite 369/369, solution + `app/` build clean, `dotnet format` clean, `--selftest` 26/26 (207/207 locale parity, unchanged — no strings touched). [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-UI-13-user-profile-window.md) |

| M5-4 | LSL / SLS Script Dialog System (`llDialog`) ✅ | ui/net | claude | ux-designer | claude | M0-3 | Modal UI popups inheriting from `SLNGWindow` with 3x4 button grid & channel reply. Live-verified against a touch_start -> llDialog test script: popup renders, button reply reaches the sim. [Spec](file:///E:/Git/SLNG/docs/specs/M5-4-script-dialogs.md) |
| BUG-UI-01 | SLNGWindow Collapse/Minimize Frame Size ✅ | ui | claude | ux-designer | claude | M5-3 | When minimizing a window, the content hides but the outer frame does not shrink to the header size. **Root cause:** `SLNGWindow` is a `Container`, so its height is floored by `GetCombinedMinimumSize()` = `max(CustomMinimumSize.Y, content min height)`; the old `ToggleMinimize` set `Size.Y = 0` but every subclass's `CustomMinimumSize` (CameraHUD 160×140, ChatWindow 400×340, …) clamped it back to full height. **Fix (`SLNGWindow.cs`, central):** minimize now saves `_preMinimizeMinSize`, drops the height floor to 0 (keeps the width floor so the title isn't truncated), and defers the collapse to `ApplyMinimizedSize` (`Size.Y = GetCombinedMinimumSize().Y` ≈ header height) so it clamps against the *post-hide* layout, not the stale one; restore puts `CustomMinimumSize`/`Size` back. `SavePersistedGeometry` persists `_preMinimizeSize` while minimized so a persisted window reopens full-height. Also fixed a pre-existing unrelated selftest miss (`ui.preferences.theme_heading` absent from `de-DE.json`). Builds, 322 tests, selftest 24/24. **Confirmed in-world 2026-08-27.** `v0.9.49-alpha`. [Spec](file:///E:/Git/SLNG/docs/specs/BUG-UI-01-window-collapse-size.md) |
| FEAT-UI-01 | Contextual Cursor Interaction States ✅ | ui/render | gemini | ux-designer | gemini | M1-3 | Viewport mouse cursor dynamically changes based on target object state (Touch, Sit, Inspect, Media). [Spec](file:///E:/Git/SLNG/docs/specs/done/FEAT-UI-01-cursor-interaction-states.md) |
| MVP2-1 | Object Interaction & Sit / Touch ✅ | render/net | claude | protocol-re | claude | M1-3 | Touch objects, sit on prims/anim-seats, stand up. Left-click executes the object's ClickAction (Touch grab/de-grab via `GridSession.ClickObjectAsync`, or Sit via `GridSession.RequestSit` when `ClickAction==Sit`) in `ObjectSelectionController.cs`; also reachable via the right-click context menu (`🪑 Sit`, `🪑 Sit Here` on ground). Seat resolution (wire-relative Position/Rotation -> world space, mirroring LibreMetaverse's own `AgentManager.SimPosition`/`SimRotation` walk) happens once in `GridSession`; `AvatarComponent.SittingOnLocalId` gates `AvatarController`'s WASD/fly/ground-clamp, and a movement key stands the avatar back up (retries on a cooldown, not a one-shot latch -- a live-tested "can never stand up again" regression, since fixed). Seated avatar rendering (`AvatarRenderer.UpdateVisual`'s `SittingOnLocalId` branch) is now source-verified against LLVOAvatar::updateRootPositionAndRotation and live-verified on a real OpenSim seat -- see the spec's "Known limitations" for the full history (three earlier attempts were wrong; the initial-looking failure of the 4th was actually one test object's own oversized sit-target offset, confirmed by a second seat rendering correctly). [Spec](file:///E:/Git/SLNG/docs/specs/MVP2-1-object-interaction-sit-touch.md) |
| MVP2-2 | Landmark Teleport & Creation ✅ | ui/net | claude | protocol-re | claude | M0-2 | *(Scoped as "Phase 1 of World Map, Minimap & Teleport" — only this slice shipped; the map/minimap/search half is **MVP2-3**.)* Teleport to a landmark inventory item (direct packet + asset-decode fallback), and Create-Landmark UI. Fixed a regression where Teleport silently did nothing (stale metadata-length check); added double-click-to-teleport and a globe prefix on landmark rows. [Spec](file:///E:/Git/SLNG/docs/specs/MVP2-2-landmark-teleport.md) |
| MVP2-3 | Minimap, World Map & Region Search ✅ | ui/net | claude | protocol-re | claude | MVP2-2 | The deferred half of MVP2-2, all four phases. **Implemented on `feature/MVP2-3-world-map-minimap-search`:** `GridSession` gained neutral map/radar DTOs (`NearbyAvatar`/`NearbyAvatarsEvent`, `MapRegionInfo` — no LMV `GridRegion`/`Simulator` crosses the boundary) and wrappers over `GridClient.Grid`: `NearbyAvatarsUpdated` (off `CoarseLocationUpdate`), `RegionDiscovered` (off `GridRegion`), `RequestMapBlocks`, `ResolveRegionByNameAsync`/`ResolveRegionByHandleAsync` (`GetGridRegionAsync`), and `TeleportToAsync(handle, localPos)` (mirrors `TeleportToLandmarkAsync`'s progress-message + position-resync plumbing). **Phase 1 Minimap:** `MinimapOverlay : SLNGWindow` (2026-08-28: promoted from a passive StatsOverlay-style corner panel to a full window once it grew a zoomable radar + clickable roster — real interactive content earns the same window chrome every other panel gets) — avatar-centred, mouse-wheel-zoomable (16–512 m visible range, default 64 m) radar of the current region with a scrollable roster list; own position/heading from `World`'s local `AvatarComponent`, nearby dots + names merged from `World` (exact, draw-distance-limited, has names) and `CoarseLocationUpdate` (coarse, region-wide, id-only — names lazy-resolved via `RequestAvatarName`, same pattern as `FriendsPanel`) keyed by agent id. Clicking a roster row highlights that avatar's dot on the radar (a red ring). **Phase 2 World map:** `WorldMapWindow : SLNGWindow` — pan (drag) + zoom (wheel) over region tiles fetched via `RequestMapBlocks` and rendered through the existing texture/`GpuCache` path, own-avatar marker only, click-to-inspect, double-click-to-teleport. **Deliberately own-marker-only, not a second avatar radar** — 2026-08-28 clarification: the world map is a grid-wide sim search/teleport tool and the minimap is the per-region avatar radar, two different tools (matching real SL/Firestorm: the World Map shows your own position, never other residents — that's the Mini-Map's job). An intermediate revision briefly added other-avatar dots to the world map too; reverted. Reachable from the bottom toolbar AND `TopMenu`'s World menu ("World Map…" / "Minimap"), not toolbar-only. **Phase 3 Search:** a search box resolves "Region Name" or "Region Name x y z" via `ResolveRegionByNameAsync`, centres the map, and arms Teleport. **Phase 4:** "Arrived in `<region>`" toast (`Boot.LogMessage`) on `GridSession.RegionConnected`, deferred until `CurrentRegionName` is actually populated (RegionHandshake lands after the connect event fires) rather than risking a blank name. Every `await`-resumed continuation (search/resolve/teleport/tile-fetch) marshals back via `CallDeferred` before touching a Control — Godot's main thread carries no `SynchronizationContext`, so a click handler's code after its first `await` is NOT guaranteed to still be on it. Toolbar entries "Minimap"/"World Map" (respect "Toggle HUD" via `hudLayer` parentage). Localised (`ui.worldmap.*`, `ui.map.arrived_in`, `ui.menu.world_map`/`ui.menu.minimap`, `ui.minimap.*`, en-US + de-DE, `--selftest` parity 206/206). 6 new `GridSessionTests` (wire-event mapping + no-connection graceful-failure), full suite 369/369, solution + `app/` build clean, `dotnet format` clean. `v0.10.2-alpha`. **2026-08-28 first live-test round found two real bugs, both fixed:** (1) minimap roster click did nothing — `MinimapOverlay._Process` rebuilt the whole roster's `Button` rows every frame (~60 Hz), and Godot's `BaseButton` only fires `Pressed` if the SAME instance survives both press and release, so a click landing between two rebuilds silently never registered; fixed by only rebuilding when membership/selection actually changes (`RefreshListIfChanged`). (2) world map teleport appeared to do nothing — the default zoom (`DefaultPixelsPerMeter`) rendered a 256 m region as a ~100 px square lost in a mostly-EMPTY canvas, so most clicks landed on non-existent neighbour grid squares; a raw handle-based `TeleportLocationRequest` to empty space isn't rejected, it just gets no reply, so `GridSession.TeleportToAsync` sat for the full 40s `AgentManager.TeleportTimeout` with nothing visible in between. Fixed by (a) fitting the home region to ~70% of the canvas by default, and (b) never firing a teleport at an unconfirmed region handle — resolve first (same gate the real viewer's `LLAgent::doTeleportViaLocation` uses via `LLWorldMap::simInfoFromHandle`, verified against slviewer source), teleport only once/if that confirms a region is actually there, with an explicit "No region here." message otherwise instead of silence. Diagnostic `GD.Print`s added around the teleport call for any future repro. **Same-day correction from the live tester:** the zoom theory above was wrong — they'd already zoomed in manually, saw the target sim, clicked it directly, still nothing. Re-examined the log: the click DID register at the Godot level (a genuine double-click, identical pixel back-to-back, plus a separate button click positioned where Teleport sits) and OpenSim's server-side `TeleportLocationRequest` handler (checked against the vendored `opensim_fetch` source) has no cross-region special case that would explain a silent failure — but LibreMetaverse's own teleport logging is deliberately silenced and this session predates the `GD.Print` diagnostics above, so whether `TeleportToAsync` actually ran, and what it got back, is genuinely not visible in that log. Added further `GD.Print`s at the click itself (`HandlePointAction`) and the click-resolve outcome (`ApplyResolvedRegion`) so the whole chain is traceable next time. `v0.10.3-alpha`. **Root cause found, same day, from those diagnostics:** the log showed `success=False message="Teleport finished"` — impossible if genuinely finished (that literal string is written only on success). The tester had clicked "Teleport" again for a DIFFERENT destination while the first attempt was still in flight — confirmed by their own follow-up ("it was the sim I wanted — a different one worked", i.e. destination didn't matter, timing did). LibreMetaverse's `AgentManager` tracks the in-flight teleport in a single shared field (`_teleportTcs`); the landmark-UUID overload guards against a second concurrent call, but the region-handle overload our map/landmark-fallback paths use does NOT — two overlapping calls silently cross-wire, a response for one completing the other's task instead. Fixed with a session-wide `Interlocked`-based `_teleportInProgress` guard shared by `TeleportToAsync`/`TeleportToLandmarkAsync`/`TeleportToGlobalPosition`, rejecting a second attempt outright with a clear "A teleport is already in progress." instead of letting both corrupt each other — the only fix possible from outside a vendored NuGet package. Also fixed the failure-message preference (favoured a transient `TeleportProgress` narration over LibreMetaverse's own authoritative final `TeleportMessage`, which is why the first live-test round saw the misleading "Teleport failed: Teleport started" for what was actually this race). `v0.10.4-alpha`. **Confirmed in-world 2026-08-28**: a single, non-overlapping map-click teleport to "Swondo Flower Island" produced `success=True` in `godot.log` followed by real region-crossing asset activity. **Same-day follow-up bug**, also fixed: the world map window opened 640x520 at a hardcoded (200,100) regardless of actual screen size and ran off the bottom — `PersistId` was being set AFTER `base._Ready()` in both `WorldMapWindow` and `MinimapOverlay`, so `SLNGWindow`'s own geometry-restore/viewport-clamp (both live inside `base._Ready()`) silently ran as no-ops; fixed by reordering (matching `EnvironmentWindow`'s already-correct pattern) and shrinking the first-open default to 600x460 at (160,60) — a manual resize now also actually persists across relaunches, which it silently didn't before either. `v0.10.5-alpha`, full suite 369/369, `dotnet format` clean. **Confirmed in-world same day**: minimap roster click-to-highlight works. **2026-08-28 addendum, requested by the tester:** double-clicking a roster row now jumps the radar to that avatar too — pans AND zooms in (32 m visible range vs. the 64 m default); `RadarCanvas` projects everything relative to a `center` parameter that defaults to the local avatar but can be overridden, so the local avatar's own dot/arrow is drawn at its true relative position rather than assumed fixed at the canvas centre. Double-clicking the already-focused row un-focuses; focus clears automatically if that avatar drops out of range. Purely client-side (no protocol representation for a 2D radar's pan/zoom — confirmed against the SL wire protocol: `AgentUpdate`'s "camera" fields are for the real 3D viewport camera, unrelated). `v0.10.6-alpha`, full suite 369/369, `--selftest` locale parity 207/207, `dotnet format` clean. **2026-08-28 follow-up, window-too-tall recurred:** `preferences.cfg` showed `world_map_size=Vector2(480, 1236)` — the real cause (the earlier reorder "fix" was a misdiagnosis, corrected in the spec) was that `SLNGWindow` only ever had ONE resize handle (bottom-right corner), coupling width and height on every drag with no way to adjust one without the other, and `RestorePersistedGeometry` only ever enforced a minimum size, never a maximum, so one bad drag reloaded broken forever. Also directly requested by the tester ("Fenster an allen Seiten anfassen"). Fixed in `SLNGWindow` itself (shared by every window in the app): all 4 edges + 4 corners now get their own resize handle, and both a live drag and `RestorePersistedGeometry` clamp against the current viewport as a maximum, so an already-broken saved size self-heals on next load. `v0.10.7-alpha`, full suite 369/369, `dotnet format` clean, `--selftest` 26/26. **2026-08-28 crash found in `godot.log`:** `System.ObjectDisposedException` ("Cannot access a disposed object. Object name: 'Godot.ImageTexture'") in `MapCanvas._Draw()`, repeated ~28,000 times in one log (once per frame). Cause: `WorldMapWindow.LoadTileAsync` called `GpuCache.GetOrUploadTextureAsync` without ever pinning the result with `AddRef` — the tile's cache entry started at RefCount 0 and was eviction-eligible (and disposed) the moment some OTHER texture pushed the shared 256MB `GpuCache` budget over the edge, even while this window was still actively drawing it every frame. Fixed by calling `GpuCache.AddRef`/`ReleaseRef` around a tile's lifetime, matching `ObjectRenderer`'s own established convention (`GetOrUploadTextureAsync` itself stays ref-neutral; explicit `AddRef`/`ReleaseRef` is the sole ref-counting mechanism, avoiding `initialRefCount`'s "first caller wins" ambiguity). `v0.10.8-alpha`, full suite 369/369, `dotnet format` clean, `--selftest` 26/26. A second, unrelated `[MainThreadWork] item threw: Cannot access a disposed object` (same exception class, different call site) turned out to be `UserProfileWindow`'s identical missing-`AddRef` pattern — fixed separately, see FEAT-UI-13's row. **2026-08-28 clarification + addition:** the double-click-jump from the earlier addendum was minimap-internal pan/zoom; the tester actually meant the real 3D camera, plus asked for a right-click context menu on a roster row. Added both: double-click now ALSO turns the in-world camera to look at that avatar (`AvatarController.FocusOnWorldPosition`, extracted from the existing Alt+Click-drag "look at this point" gesture's math so both share one implementation; `RenderConfig.ToGodot` converts the roster's SL-region-local position first), and right-click opens the SAME shared avatar context menu the in-world right-click gesture uses (`InWorldContextMenu.ShowAvatarMenu`) rather than a second one-off menu. That shared menu itself grew two entries it was missing even for the in-world case -- "Offer Teleport" and a Mute/Unmute toggle (`GridSession.OfferTeleport`/`IsAvatarMuted`/`SetAvatarMuted`, all already existed, just not wired into this menu) -- so the in-world right-click-an-avatar gesture also gained them, not just the minimap. `v0.11.0-alpha`, full suite 369/369, solution + `app/` build clean, `dotnet format` clean, `--selftest` 26/26 (207/207 locale parity, unchanged -- this menu was never localised to begin with, matching its existing style). **2026-08-28 second clarification, same day:** the double-click camera turn above just re-aimed from wherever the camera already was (`FocusOn`, reused as-is) -- correctly called out as "not just swing the camera, zoom in, frontal view." Added `AvatarController.FocusOnAvatarFrontal(Vector3, Vector3?)`: fixed close "portrait" distance (3.5 m) aimed at roughly head height, approaching from the avatar's own facing direction when known (so the shot is genuinely frontal) via a new shared `AimOrbitAt(target, fromPos, zoom)` helper both this and `FocusOn` now call. The roster's per-avatar tuple grew an optional `Rotation` (only known for `World`-tracked avatars in draw distance) to supply that facing, converted to Godot space with the same axis map `RenderConfig.ToGodot` already documents for positions. `v0.11.1-alpha`, full suite 369/369, `dotnet format` clean, `--selftest` 26/26. **2026-08-28 third clarification, same day:** "works, but I'd find it better as a pan than a hard switch." Added a generic smooth-transition layer to `AvatarController` (`StartTransition`/`UpdateTransition`, smoothstep-eased over 0.5s) that both `FocusOn` (Alt+Click) and `FocusOnAvatarFrontal` (the minimap jump) now go through via `AimOrbitAt`, instead of snapping the orbit yaw/pitch/zoom/target instantly -- Alt+Click's own "look at this point" gesture picked up the same smooth settle as a side effect. Any manual camera input (drag/pan/zoom/WASD) cancels an in-progress transition immediately. `v0.11.2-alpha`, full suite 369/369, `dotnet format` clean, `--selftest` 26/26. **2026-08-28 follow-up, "doesn't reopen at last size":** the earlier `PersistId`-reorder fix left a real bug: it added a synchronous "if Position is still (0,0), apply the hardcoded default" check right after `base._Ready()`, on the same wrong assumption that the deferred restore had already run by then -- it hadn't, so the hardcoded default fired on EVERY open, briefly overwritten a moment later by the real restore, but only after the window was already visible wrong. Fixed by moving that fallback into its own `CallDeferred`, made at the end of `_Ready()` so Godot's FIFO deferred-call queue guarantees it runs strictly after `base._Ready()`'s own restore. **Also, requested the same day:** "das sanfte Zoomen wäre auch beim Kamera-Reset toll" -- `ResetCamera`/`SetPresetView` now go through the same smooth-transition layer the minimap jump uses (`ResetCamera` is now just `SetPresetView("rear")`), ending in the dynamic avatar-follow target rather than a frozen point. `v0.11.3-alpha`, full suite 369/369, `dotnet format` clean, `--selftest` 26/26. See the spec for the complete same-day debugging trail. [Spec](file:///E:/Git/SLNG/docs/specs/MVP2-3-world-map-minimap-search.md) |

| BUG-ENV-01 | Region-environment event crashes client on login ✅ | render/net | claude | graphics-engineer | claude | FEAT-ENV-01 | Hard crash (`Fatal error. System.AccessViolationException` in `godotsharp_callable_call_deferred`) a few seconds after login. **Cause:** `Boot.OnLoginPressed` wired `GridSession.RegionConnected` and `RegionEnvironmentReceived` with `Godot.Callable.From(() => …).CallDeferred()`. Both events are raised off the main thread (network thread / threadpool continuation after the CAPS `GetParcelEnvironmentAsync` await), and a **custom delegate-backed `Callable`'s deferred dispatch is main-thread-only** in Godot .NET — calling it from a foreign thread corrupts memory. (`GodotObject.CallDeferred(nameof(Method), variantArgs)` on a real node *is* thread-safe, which is why every other cross-thread handler in `Boot` was fine.) Latent since FEAT-ENV-01 Phase D (`v0.7.0-alpha`). **Fix (`Boot.cs`):** `RegionConnected` → `CallDeferred(nameof(ApplyRegionOrigin), handle.ToString())`; `RegionEnvironmentReceived` → park the event in a field, drain once per frame in `_Process` via `Interlocked.Exchange` and call `SetCycle` on the main thread (AGENTS.md buffer-and-drain rule). Builds, 322 tests, selftest 24/24. **Confirmed in-world 2026-08-27.** `v0.9.50-alpha`. [Spec](file:///E:/Git/SLNG/docs/specs/BUG-ENV-01-region-env-event-thread-crash.md) |
| BUG-NET-03 | Neighbor Region Rendering / Cross-Region Visibility ✅ | render/net | claude | protocol-re | claude | MVP2-3 | Establish secondary circuit connections to adjacent regions so terrain and objects beyond the local 256m border are fetched and rendered. **Root cause (Phase 1, `fix/BUG-NET-03-neighbor-region-rendering`):** LibreMetaverse 3.1.3 defaults `Settings.Agent.MultipleSims` to **`false`** (`Settings/AgentSettings.cs:10`); with it off, `NetworkManager.EnableSimulatorHandler` (`NetworkManager.cs:1463`) drops **every** `EnableSimulator` the grid sends for a neighbor, so no child circuit is ever opened and the world ends at the 256 m border. Everything downstream was already multi-region: `World` keys entities/terrain by `(regionHandle, localId)`, `WorldSimulation` threads `RegionHandle` through every handler (`RegionDisconnectedEvent` → `World.RemoveRegion`), `RenderConfig.ToGodot(handle, …)` offsets by the region's global corner, `TerrainRenderer` builds one node per region, and `GridSession`'s object/terrain/kill handlers + per-sim `TerrainSettingsEvent` pass `e.Simulator.Handle` through with **no `CurrentSim` filter**; BUG-NET-01's `min(dist-to-avatar, dist-to-camera)` cull already covers "within draw distance". **Fix:** `_client.Settings.Agent.MultipleSims = true` in the `GridSession` ctor + unconditional `[Neighbor] connected/disconnected` log lines + a test pinning the flag (its default is the wrong way). `v0.11.6-alpha`; builds (solution + `app/`), 377 tests, `dotnet format` clean, `--selftest` 26/26. **Review feedback (`v0.11.7-alpha`, `fix/mvp2-review-feedback`):** where a region has *no* neighbor, the edge no longer just cuts off — a single 16 km coarse water plane (`TerrainRenderer._voidWater`, placed at the current region's water height via `SetPrimaryRegion` from `Boot.ApplyRegionOrigin`) fills the horizon so a borderless region reads as sitting in an ocean, not a platform in the skybox void. Sits 0.05 m below the per-region planes so those win where they exist; the water shader takes wave phase from world position so the overlap stays seamless. **Void-water fill confirmed in-world 2026-08-28.** **Seam gap fixed (`v0.11.8-alpha`, `fix/BUG-NET-03-terrain-seam`):** in-world the neighbor terrain DID render, but a grey canyon ran along every sim border — `TerrainRenderer.RebuildTerrain` built quads only to local x/z `width-1`/`height-1` (the heightmap has `width` samples but the region is `width` metres; the column at x==width belongs to the East neighbor), so world x∈[width-1,width] had terrain from neither region and the void water showed through. The mesh now runs one row/column further; that closing edge takes its height from the adjacent region's edge sample when loaded (seamless join) or flat-extrapolates this region's own edge otherwise (a 1 m lip, still far better than a gap). `OnTerrainSettingsUpdated` nudges the W/S/SW neighbors so a late arrival closes the seam retroactively. **Confirmed in-world 2026-08-28** — neighbor terrain renders and the seam is closed. **Crash fixed (`v0.11.9-alpha`, `fix/BUG-NET-03-neighbor-avatar-churn`):** `MultipleSims` also made neighbor sims stream *avatar* updates, including our own avatar as a child agent there with a foreign LocalId; `WorldSimulation` flipped the local agent entity's RegionHandle/LocalId back and forth on every packet, rebuilding the Bento skeleton and leaving stale `BoneAttachment3D` nodes → `ObjectDisposedException` spam from `AvatarRenderer.UpdateAttachment` (`.GetChildren()` on a freed node). Fix: the current sim is the sole authority for avatars again — `OnAvatarUpdate` / `OnTerseObjectUpdate`'s avatar branch drop non-`CurrentSim` updates (we don't render neighbor-region avatars yet), `RaiseObjectUpdate` drops attachment-flagged prims from a non-current sim, and `AvatarRenderer.UpdateAttachment` validates the cached node with `IsInstanceValid` before reuse (self-heals any freed-node race). +1 test. **Phase 2 (deferred, needs live packet capture):** whether OpenSim streams objects to a child agent with no client `AgentUpdate` (LMV's `Movement.SendUpdate` only targets `CurrentSim`; if neighbor objects don't appear, hand-roll an `AgentUpdatePacket` per child circuit); walk-across crossings may not recenter the floating origin (harmless within ±256 m). [Spec](file:///E:/Git/SLNG/docs/specs/BUG-NET-02-neighbor-region-rendering.md) |
| BUG-UI-05 | Camera Alt-Zoom Off-Center Focus & Gliding ✅ | ui/render | claude | ux-designer | claude | BUG-UI-03 | Fix wrong mouse-wheel vector calculation when Alt-Clicking an off-center object, and implement smooth gliding to new focus targets instead of instant snapping. **Requirement 2 (smooth glide) already shipped** with MVP2-3's `AvatarController` transition layer (`StartTransition`/`UpdateTransition`, smoothstep 0.5 s) — `FocusOn` (Alt+Click) routes through `AimOrbitAt`→`StartTransition`, so the focus change already eases rather than snaps. **Requirement 1 (zoom focus math)** landed on `fix/BUG-UI-05-camera-alt-zoom-offcenter`: the root cause was that every wheel/drag zoom went through `ZoomTowardCursor`, whose screen-space `_panOffset` nudge is an *approximation* for "zoom at the mouse" that only makes sense when there is **no** stored 3D pivot. With an Alt+Click focus active there IS one (`_orbitTarget`, the raycast hit point), and the nudge — plus `ZoomTowardCursor` killing the focus glide mid-interpolation via `_transitioning = false` — left the pivot frozen at a half-way `Lerp` point with an accumulating pan offset, so each scroll zoomed toward a point that was neither the clicked object nor screen centre. New `ZoomBy(zoomDelta, mousePos)` dispatches: focus active (or a non-avatar-follow glide still running) → pure `_zoom` dolly straight along the camera→`_orbitTarget` line, `_panOffset` untouched, and if a glide is in flight the whole zoom ramp (`_transitionStartZoom`/`_transitionEndZoom`) is shifted by the same delta so the pan keeps easing toward the target and just rests nearer/further instead of snapping dead; no focus → `ZoomTowardCursor` unchanged. All three zoom call sites (wheel up/down in `_UnhandledInput`, Alt+LMB vertical-drag in `_Process`) now go through it. Builds (solution + `app/`), 369 tests, `dotnet format` clean, `--selftest` 26/26. `v0.11.4-alpha`. **Confirmed in-world 2026-08-28.** [Spec](file:///E:/Git/SLNG/docs/specs/BUG-UI-05-camera-alt-zoom-offcenter.md) |
| FEAT-UI-18 | Teleport Loading Screen Overlay ✅ | ui/net | claude | ux-designer | claude | MVP2-2 | Display a loading overlay (similar to login screen) during teleportation to bridge the waiting period and show network progress. **Implemented on `feature/FEAT-UI-18-teleport-loading-screen`:** neutral `TeleportStage`/`TeleportProgressEvent` (mirrored by name from LibreMetaverse's `TeleportStatus`, `None` dropped — same boundary pattern as `LoginStage`) + `GridSession.TeleportProgress` event raised by all three teleport paths (region-handle / landmark / global-position). Each path also raises a **synthetic `Started`** (LMV doesn't reliably raise `Start` before `Progress`) and a **terminal `Finished`/`Failed`** via a `FinishTeleport()` wrapper around every `return` — a teleport *timeout* raises no LMV event at all, so an overlay listening only to relayed events would hang. New `TeleportOverlay` (`CanvasLayer` Layer 100, above the HUD; reuses `ui_blur.tres` + the login screen's navy-glass/teal look; **indeterminate** spinner ring since teleport has no percentage; `MouseFilter.Stop` backing rect blocks interaction; `Tween` fade in/out; terminal state lingers 0.4 s success / 3 s failure). `Boot` buffers `TeleportProgress` off the network thread into a `ConcurrentQueue`, drains in `_Process`, and `ApplyTeleportProgress` maps stage → overlay with a **localised** per-stage string (`ui.teleport.*`, en-US + de-DE); a real failure reason passes through verbatim. `ForceHide()` covers the disconnect/relogin edge. 7 new `GridSessionTests`; builds (solution + `app/`), 376 tests, `dotnet format` clean, `--selftest` 26/26 (locale parity 212/212). `v0.11.5-alpha`. **Review feedback (`v0.11.7-alpha`, `fix/mvp2-review-feedback`):** after arrival the camera pans back behind the avatar (`AvatarController.ResetCamera`), deferred in `Boot._Process` until the local avatar entity's region handle catches up to the destination (first `AvatarUpdate` post-teleport) so the follow target isn't stale, with a 4 s deadline guard. **Confirmed in-world 2026-08-28** — overlay + progress + auto-dismiss, and the post-arrival camera return. [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-UI-18-teleport-loading-screen.md) |

---

## MVP 3 — Avatar, Posing & Media Studio 🚧

**Goal:** Full avatar customization, animation/pose controls, shared media, and snapshot tools.

| ID | Task | Track | Owner | Agent | Tool | Dep | Done when |
|---|---|---|---|---|---|---|---|
| M4-7 | Base-mesh Hiding Under Worn Mesh ✅ | render | claude | graphics-engineer | claude | M4-3 | System head/body parts hidden per worn alpha layers & BoM rules. **Most of it already existed** (`AvatarRenderer.RegisterBomAndUpdateVisibility` = `LLVOAvatar::updateMeshVisibility`: hides `head`/`eyelashes`/`upper_body`/`lower_body`/`eye`/`hair` by consumed bake channel, self + others; Alpha wearables punch the system body via the server bake's alpha + unconditional `AlphaHash`). **Bug fixed this pass:** `AttachmentBakeChannels` was add-only — detaching a BoM mesh body/head left the system part invisible until relog. New `RecomputeMeshVisibility` rebuilds the channel set from the live `BomAttachments` survivors and re-applies part visibility on add **and** remove (`RemoveVisual` + HUD-move path now call it). Builds, tests, selftest green. `v0.9.51-alpha`. The full visual end-to-end (remove a "hide everything" alpha layer, watch the system body return) is blocked by a separate gap, not by M4-7 — see **FEAT-AVATAR-01**. [Spec](file:///E:/Git/SLNG/docs/specs/M4-7-base-mesh-hiding.md) |
| FEAT-AVATAR-01 | System Wearables — Remove / Add + Self-Rebake 🧪 | net/render | claude | protocol-re | claude | M4-3 | Taking a **system wearable** off (or on) in SLNG does nothing visible — found verifying M4-7. Two causes: (1) `GridSession.DetachItemAsync` only calls `Appearance.Detach` (`DetachAttachmentIntoInv`), which the sim applies to **attachments** only; a wearable (Alpha/Skin/Shape/Tattoo/Universal) is a no-op and stays worn server-side — there's no `RemoveFromOutfit`/`ReplaceOutfit` path, and the add side has the mirror gap. (2) `SendAppearance = false` (set after the 2026-08-02 "avatar deformed, in FS too" incident — `MyVisualParameters` was empty) means LibreMetaverse never computes/uploads a bake or sends `AgentSetAppearance`, so no rebake ever happens. Net: baked textures are frozen at login; `UpdateVisual` step 3 never re-fires for self. **Not** a regression and **not** M4-7. Landmine: don't just flip `SendAppearance` — `LogVisualParamHealth()` must read a sane 218-param set first. **Wearable remove/add WORKS (`v0.14.1-alpha`)** — via the Current Outfit Folder + `AgentIsNowWearing`, bypassing `AppearanceManager` entirely. Wear = `CreateLinkAsync` into the COF; remove = delete the matching COF link(s); both then send `AgentIsNowWearing` with the worn set read from the COF. That packet carries **item ids and wearable-type bytes and nothing else** — no visual params, no texture entry — so it cannot write a wrong shape or strip a bake, and OpenSim's `AvatarFactoryModule` applies + persists it (`QueueAppearanceSave`, `AvatarFactoryModule.cs:1251-1254`). The Outfits tab's "Von aktuellem Outfit entfernen" now covers wearables too (its "Phase 2" limit existed only because they couldn't be removed). **Remaining: the bake.** The change is stored but not visible until a viewer composites new baked textures — a SLNG relog replays the stored bake, a Firestorm login or Ctrl+Alt+R applies it everywhere. So the open work is exactly one low-risk render feature: SLNG needs to bake (a bake only uploads textures, it writes no shape). **Background — the root cause of every avatar corruption since July:** `AppearanceManager.MakeAppearancePacket` iterates `VisualParams.Params` (all 672) and takes the first 218, but the wire order is `Group0ParamIds` (the 253 transmitted ids) — they agree for 23 slots, so **195 of 218 values land on the wrong parameter**. Pinned by `SLNG.Assets.Tests.VisualParamOrderTests`, verified against the reference viewer, which filters by param group where LibreMetaverse does not. `SendAppearance` stays `false` and every `AppearanceManager` send path is a deliberate no-op. Also fixed en route: `OnAppearanceSet` no longer feeds encoder-order params to the renderer, and the Worn tab reads live state from the COF (a COF link IS the worn state for a wearable) instead of marking most of the outfit inactive. [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-AVATAR-01-wearable-remove-add-rebake.md) |
| FEAT-AVATAR-02 | Avatar Health & Troubleshooting Tools | render/ui | | graphics-engineer | | MVP3-1 | Implement tools for avatar recovery (Rebake Textures, Stop Animations, Reset Skeleton / Undeform Avatar) in a dedicated menu. [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-AVATAR-02-avatar-troubleshooting-tools.md) |
| M5-1 | Inventory Browser v1 — Read-only ✅ | net/ui | claude | ux-designer | | M0-2 | Lazy tree view of folders & items |
| MVP3-1 | Inventory v2 — Wear, Detach & Drag-Drop ✅ | net/ui | gemini | ux-designer | gemini | M5-1 | Wear/detach clothing & attachments, move/delete items. [Spec](file:///E:/Git/SLNG/docs/specs/done/MVP3-1-inventory-wear-detach.md) |

| FEAT-INV-02 | Give Inventory Items via Chat / IM Window ✅ | net/ui | gemini | ux-designer | gemini | MVP3-1 | Give items/folders via IM chat tabs & Drag-and-Drop with permission checks. [Spec](file:///E:/Git/SLNG/docs/specs/done/FEAT-INV-02-give-inventory-via-chat.md) |
| MVP3-3 | Shared Media / MOAP (Media on a Prim) | render/net | | graphics-engineer | claude | M2-3 | Web browser / video streaming on prim faces |
| MVP3-4 | Snapshot & Photography Studio 🚧 | ui/render | claude | graphics-engineer | claude | M2-5 | High-res screenshot capture, DoF, FOV control, EEP/environment presets. Staged in 4 phases. **Phase 1 landed** (`v0.9.53-alpha`): `SnapshotWindow : SLNGWindow` + toolbar entry (`add_a_photo`); "Capture" hides the whole `HudLayer` for one rendered frame (`ProcessFrame` → `FramePostDraw`) and reads back the main viewport → world-only image, shown as a preview; "Save PNG" writes to `user://snapshots/snapshot_<timestamp>.png` (dir auto-created, `_n` suffix guards collisions) and reports the path. Native resolution for now. **Confirmed in-world 2026-08-27** — capture produces a UI-free image, save writes the PNG. Phases 2–4 (resolution multiplier via a mirrored `SubViewport`, FOV + DoF **FEAT-RENDER-07**, day-cycle presets over `EnvironmentDriver`) **deferred** — basic capture works and doesn't block other MVP 3 work. [Spec](file:///E:/Git/SLNG/docs/specs/MVP3-4-snapshot-photography-studio.md) |
| FEAT-RENDER-07 | Depth of Field (DoF) Camera Effect | render | | graphics-engineer | | MVP3-4 | Cinematic blur effect (auto/manual focus). Part of the Photography Studio. [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-RENDER-07-depth-of-field.md) |
| FEAT-ENV-01 | Region Environment — Windlight / EEP 🚧 | net/core/render | | protocol-re | | M0-2 | The **data half** of Windlight/EEP: fetch the region environment, model it engine-neutrally, evaluate the day cycle, publish it as global shader uniforms. Today `Boot.SetupEnvironment` hardcodes one `ProceduralSkyMaterial` + fixed Godot fog, so every region on every grid looks identical; only the sun *direction* is live (`SimulatorViewerTimeMessage`). The protocol side needs no new dependency — LibreMetaverse **3.1.3, the version already pinned**, ships `EnvironmentManager` as `GridClient.Environment` with `GetRegionEnvironmentAsync` / `GetLegacyEnvironmentAsync` and change events, and both caps (`ExtEnvironment`, `EnvironmentSettings`) are already in its requested set; confirmed by compiling a probe against the exact package rather than reading the newer vendored checkout. LibreMetaverse hands back the settings as **raw LLSD** — parsing them is our job, against the key names pinned in `llsettingssky.cpp:72-113` and `llsettingswater.cpp:36-48`. The port target is `class1/windlight/atmosphericsFuncs.glsl`'s `calcAtmosphericVars`, whose `sunlight_color`/`ambient_color` uniforms are **derived**, not raw LLSD — `LLSettingsSky::calculateLightSettings` (`llsettingssky.cpp:1704`) is its CPU-side twin and skipping it is the classic wrong port. The advanced-atmospherics fields (`rayleigh_config`, `mie_config`, `absorption_config`, `planet_radius`) are out of scope by measurement: they reach no shader anywhere in the viewer tree. Staged A–E, first three phases with zero visual change; Phase A exists purely to capture what OSGrid actually returns instead of designing the fallback chain against a guess. Phase E is FEAT-RENDER-01 Phase 5 and is blocked on its Phases 3–4 — atmospherics on prims but not avatars/terrain/water is the exact seam ADR 0002 exists to avoid. **Phases A–C landed** (`v0.6.2-alpha`): capability probe + raw LLSD capture to `user://logs/`, the engine-neutral `SkySettings`/`WaterSettings`/`SkyLighting` model, the `EnvironmentLlsdParser`, and pure day-cycle evaluation — 29 tests. **Phase D landed** (`v0.7.0-alpha`): `EnvironmentDriver` now drives sun colour/energy, ambient (had to flip `AmbientLightSource` from `Sky` to `Color` — Godot silently ignores the color property under `Sky` mode), the `ProceduralSkyMaterial` dome, classic fog (with `VolumetricFogEnabled` turned off to stop it double-applying against a hardcoded density that never moved), and the water plane's colour — all once per frame, no shader touched. The sky-dome/fog mapping is a documented approximation, not a port of `calcAtmosphericVars` — there's nowhere to port it into before Phase E's shader seam is real. Headless-boot clean; **not yet watched on a live region**. Two things the implementation measured and the design had to absorb: the pinned 3.1.3's `EnvironmentData` has no `SkyTrack` (vendored-checkout-only), and `EnvironmentManager` raises its Updated events *only* from inside its own GETs — it registers no EventQueue callback, so there is no push notification and a server-side environment change never reaches us until something re-polls. **Confirmed live 2026-08-19** on OSGrid's The Dangazi Forest: the region returns a full EEP day cycle via `ExtEnvironment` (21 sky frames, 3 water frames, 33 keyframes, water on track 0 and ground sky on track 1). The load-bearing finding is that **all 21 sky frames nest their haze under `legacy_haze`** and none carry it at the top level — a parser reading only the top level would render the region with default haze, silently. The capture is committed as a fixture (`tests/SLNG.Net.Tests/TestData/`) and parsed on every test run. [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-ENV-01-windlight-eep.md) |
| FEAT-ENV-02 | Windlight Preset Picker ✅ | ui/net/render | claude | ux-designer | claude | FEAT-ENV-01 | The **user half** of Windlight: pick a sky/water preset instead of the region's, and get back to the sim's environment with one click. Ships the viewer's legacy preset library (`app_settings/windlight`: 36 skies, 7 water settings, the same files Firestorm offers) as LLSD in `app/assets/windlight` — no grid needed, applicable before login. `World ▸ Umgebung (Windlight)…` and a `wb_sunny` toolbar button open `EnvironmentWindow`; selecting an entry applies it immediately, sky and water override independently (picking a sky keeps the region's water), and the driver keeps the region cycle underneath so "use region setting" needs no refetch. The load-bearing part was the legacy→EEP conversion the viewer does in `translateLegacySettings` (llsettingssky.cpp:988-1113) and that `EnvironmentLlsdParser` had never needed: legacy stores every scalar **array-wrapped** as `[value,0,0,1]` and `OSD.AsReal()` on an array returns **0 silently** — so `cloud_scale`, `cloud_shadow`, `gamma`, `max_y` and the entire haze block read as zero, which also means **the live legacy-Windlight region path had this bug all along**. Plus `sun_angle`/`east_angle` → `sun_rotation` (a legacy setting carries no sun quaternion at all; without it every preset pins the sun on the horizon due east), star brightness ×250, and `normalMap` as an alias of `normal_map`. The sun conversion is verified against the `lightnorm` each preset file carries — the direction it was authored with. Tests parse the REAL shipped files (linked into the test project, not copied), 110 Net tests; `--selftest` loads the whole library through Godot's own filesystem, so a missing export include fails a check instead of showing an empty picker (26/26). `v0.9.60-alpha`. **Confirmed in-world 2026-08-29** — picking a sky/water preset applies immediately, sky and water override independently, and "use region setting" returns to the sim's environment. [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-ENV-02-windlight-preset-picker.md) |
| FEAT-RENDER-01 | Custom Spatial Shader Family for World Surfaces 🚧 | render | | graphics-engineer | | M2-3 | Replace `StandardMaterial3D` across `ObjectRenderer`/`AvatarRenderer`/terrain/water with one custom Godot spatial-shader family: UV scale/offset/**rotation** in the vertex shader, plus an atmospherics `#include` seam (no-op at first) fed by global shader uniforms. Forced by two findings: SL face rotation is unrepresentable in `StandardMaterial3D` (49 faces at exactly π/2 measured in one view of OSGrid's Dangazi Forest, visibly mis-placed vs Firestorm), and Windlight/EEP needs per-fragment atmospherics on *every* surface, which `StandardMaterial3D` has no seam for — so a hybrid is a dead end. Highest blast radius so far; staged in 5 independently verifiable phases, Phase 1 being a visually-identical swap. **The "misplaced texture" half is closed** (2026-08-18): it was SL's PLANAR texgen, decoded but never applied, never reported because the diagnostic compared the raw wire value against 1 when planar is 2, and dropped entirely for prims that send no per-face entries. `slng_planar_uv` now ports `planarProjection` (llface.cpp:88-120) into the vertex stage; live-verified against Firestorm on a known-answer probe object (`tools/testassets/`), matching on both tile count and phase. The probe's full case table was then stepped through against Firestorm with no divergence — which also puts Phase 1's rotation pivot and transform order on a visual footing (cases 4/11/12) where they had only been derived algebraically. The "blurry" half was already closed as a truncated asset. Spherical/cylindrical texgen need no implementation: they occur in the viewer source only in the enum declaration (lltextureentry.h:80-81), so the real viewer ignores them too — our fallback to the mesh UVs is parity, not a gap. Sculpt textures were additionally found to be VERTICALLY MIRRORED (fixed 2026-08-18, settled by A/B against Firestorm; confirmed on the Dangazi statue and the sculpt probe). **Still open:** a small constant offset remains on both UV axes for sculpts (~3 grid rows in V, U unmeasured), independent of repeat, zoom and texture resolution. The asset side is excluded by measurement — `ViewerSculptParityTests` now pins positions, UVs AND their pairing at 0.00000 against a port of the viewer algorithm on five real maps. [Handover](file:///E:/Git/SLNG/docs/handover-2026-08-18-sculpt-texture-offset.md) — read its "ruled out" section before forming a new hypothesis; six were wrong, all of them derived rather than measured. [ADR 0002](file:///E:/Git/SLNG/docs/adr/0002-custom-spatial-shader-family.md) · [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-RENDER-01-custom-spatial-shader-family.md) |
| FEAT-RENDER-02 | Terrain Detail-Texture Blending Parity ✅ | render | claude | graphics-engineer | claude | FEAT-RENDER-01 | Terrain detail-texture blending did not match the viewer, found 2026-08-21 in an A/B against Firestorm 7.2.4 on OSGrid's Howletts with the neutral EEP parity preset applied (sky, water and lighting matched; the ground pattern visibly differed). Our shader blended detail0/1/2 by elevation with three hardcoded ±2 m `smoothstep` bands and put detail3 on steep faces via a slope test. The viewer does neither: `LLVLComposition::generateHeights` turns elevation into ONE continuous composition value in [0,3] indexing all four textures, blended between neighbours by `terrainF.glsl`, and there is **no slope/cliff texture anywhere in the viewer** — our `cliff_weight` was an invention. The dominant visible difference was the missing `twiddle`: Perlin noise added to elevation *before* the band lookup, which is what makes the viewer's transitions organically mottled instead of geometric contours (the low-frequency octave alone swings the effective height by ~±9 m at a ~22 m scale, large next to typical `height_range`). Four source traps had to be cleared, each of which yields a plausible-but-wrong pattern: (1) `indra/llmath/llperlin.cpp` is **dead code with no callers** — the live noise is `indra/newview/noise.cpp`, and only it seeds `srand(42)`; (2) `scratch/noise2D.glsl` is Ashima **simplex** noise, while the viewer uses classic gradient Perlin on a 256 lattice with the cubic ease — same constants, different pattern; (3) the noise is **purely horizontal** — `generateHeights` fills `vec[2] = height/zScale` but `noise2`/`turbulence2` read only two components, so elevation never enters it and `zScale` is dead; (4) the tables come from `srand(42)` + the C runtime's `rand()`, so they differ between an MSVC- and a glibc-built viewer — Windows is the parity target, and `SlPerlinNoise.MsvcRand` is pinned by a known-answer test. Also fixed: detail UVs repeated every 5 m against the viewer's `RenderTerrainScale` default of 12 m, making every detail texture 2.4× too small. `IMG_ALPHA_GRAD_2D` turned out **not** to be a grid asset — the viewer preloads it from its own skin folder and it is absent from OpenSim's `TexturesAssetSet` — so the ramp is reproduced as the `smoothstep` that fits the shipped gradient's mean curve to 3.7/255 (a linear ramp is off by 32.6/255), redistributing no Linden art; its slow V-axis drift is not modelled and is second-order. The parity math lives in `app/materials/sl_terrain_composition.gdshaderinc` **specifically so FEAT-RENDER-01 Phase 4 can include it** rather than rewrite it. Built, 215 tests green, headless shader-load clean. **Not yet seen on a live region — the visual A/B is what closes this.** **CLOSED 2026-08-24 (`v0.9.8-alpha`), confirmed in-world.** The remaining difference was never in the maths: it was TWO texture-coordinate flips, found five measurement rounds apart, both the same convention -- SL's texture V is bottom-origin, Godot's is top-origin. `sl_detail_uv` sampled every detail texture upside down (the s/t plane equations already matched `lldrawpoolterrain.cpp:242-248` exactly; what was missing is the `v = 1 - v` that `ObjectRenderer.BuildArrayMesh` applies to every world mesh, and terrain never went through that path because it carries no mesh UVs at all). `sl_terrain_ramp` sampled the blend ramp's V the same way -- and that one bites harder, because the ramp's rows are not interchangeable (row 128 crosses over in 62 texels, row 160 takes 137), so `v -> 1-v` changes WHERE each row is used across a ~155 m field. **Why five rounds of measurement missed both:** every number this task produced is computed from position -- composition values, area splits, the heightmap comparison, the independent port agreeing to 221/224 cells -- and a texture-space flip is invisible to all of it. The maths was right the whole time. Both were caught by deliberate asymmetry instead: the detail flip by the probe texture's corner marker (built for exactly this), the ramp flip by a new known-answer heightmap (`tools/testassets/gen_terrain_map.py`) whose flat 50 m terrace sits at composition 0.50, where height contributes a constant and nothing but the blend decides the pixel, plus six known-answer poles (`band_markers.lsl`) that also ruled out a sign flip, a y-mirror and an x/y transpose. One item left open on purpose: `terrainF.glsl` samples the ramp's ALPHA channel and we sample `.r` -- our shipped PNG is greyscale with no alpha so `.r` is the only data there and it now matches in-world, but the hand extraction was never verified. [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-RENDER-02-terrain-detail-blending-parity.md) |
| FEAT-RENDER-04 | Legacy Blinn-Phong Materials (normal + specular) ✅ | net/assets/render | claude | graphics-engineer | claude | FEAT-RENDER-01 | SL faces can carry a **legacy "Blinn-Phong" material** (the 2013 materials system): a normal map and a specular map on top of the diffuse texture, plus glossiness, environment intensity and an alpha mode. We read none of it, so such a face renders as its flat diffuse texture alone. Found on OSGrid's Dangazi Forest 2026-08-23: a reef rock rendered smooth next to Firestorm's sharply layered strata. The diffuse texture was **ruled out by measurement**, not assumed — decoded from our own cache, `79bdad31` is a directionless rock (gradient energy 45.7 across X vs 46.8 across Y) and is exactly what we draw; Firestorm's Texture tab then showed the Blinn-Phong panel with both maps bound and glossiness 51. Not the same thing as the glTF PBR materials we already support: a face carries two independent ids, and we read only `RenderMaterialID` (glTF) while `MaterialID` (legacy) is never touched. Materials are not ordinary assets — they come from the region's `RenderMaterials` capability as zlib-compressed LLSD (`Zipped`), GET for all or POST for ≤50 ids (`llmaterialmgr.cpp:47-59`), with every float transmitted as an int scaled by 10000 (`llmaterial.cpp:61`). **LibreMetaverse already implements that whole path** (`Materials.LegacyMaterial`, `ObjectManager.RequestMaterialsAsync`), so the work is boundary conversion, batching/caching and rendering rather than protocol. The one part that can look plausible and be wrong is specular-glossiness → Godot's metallic-roughness; port it from the viewer's material shaders instead of fitting by eye. Staged in 5 phases, the first two with no visual change. **Phases 1-4 landed and confirmed in-world 2026-08-23** (`v0.8.0-alpha`), A/B'd against Firestorm on the reef rock: normal map, specular, and both maps' independent placement. Two prerequisites were found along the way and are worth knowing about on their own. (1) **Mesh tangents did not exist** -- `SurfaceTool.GenerateTangents()` had been disabled over a Vulkan NaN crash on degenerate triangles, so Godot had no tangent basis and could not apply ANY normal map, including the ones the glTF path already bound; `MeshTangents` computes them with finite output as its defining property. (2) **Sculpts were sampled on the wrong grid** -- PrimMesher halves both map dimensions together, the viewer spends its vertex budget in the map's actual proportion, and on the rock's 512x16 map that was 645 vertices against 1230, halving the resolution along the axis the strata run along; a reference cube rested on the surface here and sank into it in Firestorm. `SlSculptResolution` ports `sculpt_calc_mesh_resolution` and the extents now match to the last decimal. The existing parity test could not see it: it compares the surface with a 5% Hausdorff tolerance. **Phase 5 landed** (`v0.9.52-alpha`): `BuildFaceMaterialAsync` now reads the legacy material's `DiffuseAlphaMode` and picks the shader family directly — `Blend`→Blend, `Mask`→Scissor (`AlphaMaskCutoff/255` threshold), `None`→Opaque (without downgrading a translucent per-face tint), `Emissive`→Blend (fullbright proper is FEAT-RENDER-06); a `legacyAlphaModeResolved` flag then skips `ApplyAlphaCutout`'s `DetectAlpha()` pixel guess for that face. `Default` keeps the old behaviour. Mirrors the glTF branch's `pbr.AlphaMode` handling. Render-only; build + 322 tests + selftest green. **Confirmed 2026-08-27** — no regression on the reef rock or other legacy-material content; the "guess was wrong" edge case (declared `Mask`/`None` over a graded-alpha texture) wasn't encountered, so the fix is invisible where the guess was already right. [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-RENDER-04-legacy-blinn-phong-materials.md) |
| FEAT-RENDER-05 | Attachment-Point Offset Scales With the Joint ✅ | render | claude | graphics-engineer | claude | M4-8 | An SL attachment point is not its joint's origin — "Skull" (the point prim hair uses) sits 0.15 m above mHead. That 0.15 m is a JOINT-LOCAL position, so SL's joint rule applies to it: `LLXformMatrix::update` scales a local position by the PARENT's scale (`mWorldPosition.scaleVec(parentScale)`), which `ApplyShape` already does for every bone in the skeleton (`slPos *= parentScale`). The attachment point did not, so it sat at a fixed 0.15 m regardless of head size. Found 2026-08-23 from a user report that the scalp showed through prim hair ("Adam Hair(black)") next to Firestorm. The report read as a size problem and the first two hypotheses were both wrong: an alpha-cutoff blotching theory (tested live, no change) and a rigged-mesh bone-scale theory (dead on arrival — the log showed the hair is 125 ordinary prims on the Skull point, `sculpt=False`, not a rigged mesh at all, so it cannot follow head morphs in ANY viewer). On this avatar mHead's own scale is 1.1, so the hair sat 1.5 cm too deep in the skull; the error grows with head size, which is exactly why it reads as "the head is too big for the hair". Confirmed in-world. Two prerequisites came out of the same hunt: `--diag` now sets `Logger.CurrentLevel` to `Debug` — nothing in the project ever set it below `Info`, so AvatarRenderer's 11 `Logger.Debug` sites ([RiggedMesh], [Attachment], the per-mesh rigging diagnostics) were unreachable in EVERY build configuration and the log could not contain the answer; and `tools/run-client.ps1` gained a `-Diag` switch that passes the flag after the bare `--` where `Diagnostics.Initialize` looks for it. New `[HeadSize]` line reports mHead/mSkull own scale and the morphed head mesh's size in metres. |
| FEAT-RENDER-06 | Fullbright Material Property ✅ | render | claude | graphics-engineer | claude | FEAT-RENDER-01 | Fullbright-Flag (`LLTextureEntry::getFullbright`) auslesen und im Custom-Shader abbilden: das Face ignoriert die Szenenbeleuchtung und rendert in voller unbeleuchteter Texturfarbe (Schilder, Screens, Neon). **Wire→Modell:** neues `FaceTexture.Fullbright` + `PrimitiveComponent.Fullbright` (Default-Face) + `ObjectUpdateEvent.Fullbright`; `GridSession` liest `f.Fullbright` je Face und `defaultFace?.Fullbright`; `WorldSimulation` reicht es durch (Create + Update-Pfad). **Shader:** `uniform bool fullbright` in `prim_common.gdshaderinc`; am Ende von `slng_shade` `out_emission += out_albedo; out_albedo = 0` — Beleuchtung/Ambient/GI tragen dann nichts bei, Fog bleibt (wie die Viewer-Fullbright-Shader). No-op für die `unshaded` HUD-Varianten. `ObjectRenderer` **und** `AvatarRenderer` (getragene Mesh-Faces) setzen den Parameter. 3 neue Unit-Tests (`FullbrightTests`) pinnen den Wire→Modell-Pfad. Build (Solution + `app/`) + 325 Tests + `dotnet format` + `--selftest` grün (Prim-Shader 33→34 / 34→35 Uniforms, Varianten weiter deckungsgleich). `v0.9.54-alpha`. **Confirmed in-world 2026-08-27** — Fullbright-Objekt rendert unabhängig von der Tageszeit voll hell, normale Faces unverändert. **Regression am selben Tag gefunden und behoben (`v0.9.59-alpha`):** getragene HUDs rendeten komplett schwarz. Der Kommentar im Include behauptete, die Albedo→Emission-Umleitung sei "a no-op for the `unshaded` HUD variants, whose output is ALBEDO + EMISSION" — das ist falsch, Godots `unshaded` gibt **nur** ALBEDO aus und verwirft EMISSION. Da HUD-Faces praktisch immer fullbright sind, blieb ALBEDO=0 → schwarze Rechtecke, obwohl jedes Face korrekt texturiert war (in-world gemessen: 6/6 Faces, 512–1024 px, Tint weiß/grün, alle `fullbright=True`). Die drei `*_hud.gdshader` falten Emission jetzt selbst zurück (`ALBEDO = albedo + emission`); für nicht-fullbright Faces ist Emission 0 oder eine echte glTF-Emissive, die auf einem Overlay ohnehin sichtbar gehört. Die Annahme war nie geprüft worden — der visuelle Test von FEAT-RENDER-06 lief gegen ein Welt-Objekt, nicht gegen ein HUD. Neue unbedingte `[HudFace]`-Diagnose (dedupliziert, ein paar Zeilen pro Sitzung) hat den Fall entschieden: sie unterscheidet "Face hat keine Textur-ID" (loggte vorher nur via `Logger.Debug`, ohne `--diag` unerreichbar) von "Fetch/Decode lieferte null" von "texturiert" — genau die Unterscheidung, die zwei Diagnose-Runden gekostet hat. **Confirmed in-world 2026-08-27.** [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-RENDER-06-fullbright.md) |
| FEAT-UI-09 | Worn HUD Attachments Respond to Clicks ✅ | ui/render | claude | ux-designer | claude | M4-9 | User-reported: a worn AO HUD renders but does not react to being clicked. The click path turned out to be intact all along -- collider with an `EntityId` meta, ray into the viewport's own World3D via `FindWorld3D()`, `ClickObjectAsync` on a hit -- and one session proved it by logging both `swallowed by GUI control 'LogPanel'` and `clicked entity 4c7a2bc5...` side by side. Only the bottom strip of the window was dead. **Cause:** `LogPanel` is not inside `%LoginScreen`, so the boot log survived the login and stayed in-world as a bottom-anchored, full-width 150 px band -- visible clutter over the avatar AND, with a Control's default `MouseFilter`, a sink for every click landing in it. Now hidden when the loading screen is dismissed (restored if a failed login brings the login screen back), and `MouseFilter.Ignore` on it and its container regardless. **What actually took the time was finding it:** `TryClickHud` had SIX silent exits plus the `_Input` GUI guard, so "the HUD does not react" and "the click never reached the HUD code" produced identical logs. Each exit now names itself, and the guard names the Control that swallowed the click -- which is what turned four candidate causes into one line. A first pass got the visibility backwards (failures behind `--diag` while the success line printed unconditionally, making a broken click quieter than a working one) and cost a round trip; all seven print unconditionally now, a click being user-initiated and rare. `v0.9.12-alpha`. **Confirmed in-world 2026-08-25** -- the AO HUD responds across the whole window, including the strip the boot log used to occupy. |

---

## MVP 4 — Building & Creator Suite ⏸️

**Goal:** Complete in-world building, editing, prim linking, and terrain manipulation tools.

| ID | Task | Track | Owner | Agent | Tool | Dep | Done when |
|---|---|---|---|---|---|---|---|
| M5-2 | In-World Object Selection & Editing 🚧 | render/ui/net | claude | graphics-engineer | claude | M1-3 | Raycast selection, context menu, inspector tabbed window (General/Object/Features/Texture/Content) — **merged and on `main`** (`ObjectEditWindow.cs`, `ObjectSelectionController.cs`, `InWorldContextMenu.cs`; the `feature/M5-2-object-editing` branch it was written on is gone). Status kept at 🚧 for one reason only: 3D transform gizmos are still outstanding, tracked separately as FEAT-UI-04, so transform edits remain spinbox-only. [Spec](file:///E:/Git/SLNG/docs/specs/M5-2-object-editing.md) |
| FEAT-UI-04 | In-World 3D Transform Gizmos | render/ui | | graphics-engineer | claude | M5-2 | Interactive 3D translation/rotation/scale handles in viewport. Not started — no `SelectionGizmo3D` yet; transform edits currently spinbox-only in the inspector. [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-UI-04-transform-gizmos.md) |
| FEAT-UI-05 | Multi-Select & Prim Linking/Unlinking | render/ui/net | | graphics-engineer | claude | M5-2 | Box-select/Shift-select multiple prims, link/unlink root & child objects. [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-UI-05-multi-select-linking.md) |
| FEAT-UI-06 | Edit Linked Parts & Child Prims 🚧 | render/ui/net | | graphics-engineer | claude | M5-2 | "Edit linked" checkbox mode to transform individual child prims inside a linkset. Groundwork landed on `feature/M5-2-object-editing` (`SelectionSettings.EditLinkedParts`). [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-UI-06-edit-linked-parts.md) |
| MVP4-1 | Prim Rezzing & Mesh Import | render/net | | graphics-engineer | claude | M5-2 | Create new prim shapes in-world, upload/import custom meshes |
| MVP4-2 | Land & Terrain Sculpting | render/core | | graphics-engineer | gemini | M1-2 | In-world raise/lower/flatten terrain tools |

---

## MVP 5 — Economy, Voice & Marketplace ⏸️

**Goal:** Advanced ecosystem features: L$ economy, voice chat, and marketplace integration.

| ID | Task | Track | Owner | Agent | Tool | Dep | Done when |
|---|---|---|---|---|---|---|---|
| MVP5-1 | Spatial Voice Chat (Vivox / WebRTC) | net/render | | protocol-re | claude | MVP2-1 | 3D positional voice, group voice, mic input controls |
| MVP5-2 | L$ Transactions & In-World Economy | net/ui | | protocol-re | claude | M0-2 | View balance, pay avatars/objects, buy land/items |
| MVP5-3 | In-Viewer Marketplace Browser | ui/net | | ux-designer | gemini | MVP5-2 | Integrated web marketplace, direct delivery & unpacking |

---

## MVP 6 — Multi-Platform, Polish & Compliance ⏸️

**Goal:** Global readiness, performance optimizations, TPV compliance, and multi-platform deployment.

| ID | Task | Track | Owner | Agent | Tool | Dep | Done when |
|---|---|---|---|---|---|---|---|
| FEAT-UI-02 | Client Localization & Multi-Language (i18n) ✅ | ui | gemini | ux-designer | gemini | M0-4 | Full UI text translation pipeline & multi-language support. [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-UI-02-localization-i18n.md) |
| MVP6-1 | Performance Hardening & Occlusion Culling | render/assets | | performance-engineer | gemini | M2-4 | Impostors, aggressive culling, draw-call reduction on busy sims |
| MVP3-2 | Pose Stand & Animation Override (AO) ⏸️ | render/ui | | graphics-engineer | gemini | M4-2 | Play/stop custom poses, pose stands, basic AO system. **Moved out of MVP 3 (2026-08-27):** viewer-side AO deprioritized by the user (scripted AO HUDs already work via M4-2 animation playback); the pose-stand slice — freezing the avatar for a photo — folds into MVP3-4 Phase 3. |
| FEAT-UI-17 | Snapshot Saving Options & Upload ⏸️ | ui/net | | ux-designer | | MVP3-4 | **Deferred out of MVP 3 (2026-08-27) — belongs in a later milestone.** Destination + format control for the snapshot capture (`MVP3-4` Phase 1 saves one fixed PNG path). **Local disk:** file dialog for dir/filename, configurable default folder in Preferences. **Inventory:** upload the shot as a Texture asset into inventory, with an up-front L$ upload-fee warning and non-silent handling of insufficient funds / grid rejection. **Format:** PNG / JPG dropdown + JPG quality slider (`Image.SavePng` / `SaveJpg`). Extends `SnapshotWindow`. [GitHub #35](https://github.com/ClifHofmann/SLNG/issues/35) · [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-UI-17-snapshot-saving-options.md) |
| FEAT-PERF-03 | General Rendering Performance (Framerate) ✅ | render | gemini | performance-engineer | | FEAT-PERF-01 | Address low FPS (e.g. 30fps) on busy sims. Investigate shadow (CSM) cost, draw-call batching/instancing, and lack of LODs. 2434f0d: Fixed rotation stutter (disabled physics interpolation for AvatarController/Root) and local avatar network freezes (increased extrapolation limit to 5s). |
| MVP6-2 | TPV Policy Compliance Pass & Registration | infra/net | | architect | claude | M0-2 | Full Linden Lab Third-Party Viewer Policy audit & official registration |
| MVP6-3 | Cross-Platform Build Pipeline (Linux & macOS) | infra | | DevOps | claude | M1.5-3 | Native Linux & macOS releases packaged in CI/CD pipeline |
| MVP6-4 | Mobile Target Optimization (Android / iOS) | render/ui | | graphics-engineer | gemini | MVP6-3 | Touch controls UI, mobile shading profile & lower memory footprint |

---

## Backlog / Post-MVP ⏸️

**Goal:** Quality of Life (QoL) improvements and non-blocking nice-to-have features that do not fit into the core MVP milestones.

| ID | Task | Track | Owner | Agent | Tool | Dep | Done when |
|---|---|---|---|---|---|---|---|
| FEAT-UI-14 | Teleport History | ui/net | | ux-designer | | MVP2-2 | Track and persist recently visited locations (regions/coordinates). Allow double-clicking to teleport back. [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-UI-14-teleport-history.md) |
| FEAT-UI-15 | Conversation History (Recent IMs / Local Logs) | ui/net | | ux-designer | | M5-3 | Automatically save local chat and IMs to disk. Display "Recent" conversation partners in chat UI and restore previous context on IM open. [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-UI-15-conversation-history.md) |
| FEAT-UI-16 | Worn Items Inventory Tab ✅ | ui | claude | ux-designer | claude | MVP3-1 | Dedicated "Worn" (Angezogen) tab in the inventory listing currently attached/worn items for quick detachment. **Implemented `v0.11.10-alpha`, merged `v0.11.14-alpha`:** a `TabBar` splits the inventory window into **Inventar** (the existing lazy folder tree) and **Angezogen** — a flat `Tree` grouped into Körper / Kleidung / Anhänge / HUDs, each row showing name + attach point, with "(nicht aktiv)" for an item that only has a Current-Outfit link but isn't actually attached. Right-click / double-click → **Ablegen** (`DetachItemAsync`, then refresh). New neutral `GridSession.GetWornItems()` (merges live attachments + live wearables + leftover COF links, `WornItem`/`WornCategory` in `SLNG.Core`) and a `WornItemsChanged` event off `OnAppearanceSet` / self `OnAvatarAppearance`; the tab also polls on a 2.5 s timer while visible (attachment attach/detach has no clean LMV event). The pinned "Angezogen (Current Outfit)" folder that used to be the poor-man's worn view was dropped from the tree (`f540876`). **Name resolution (`5537478`):** rows showed "(unbenannt)" because `GetWornItems` read names only from the lazily-loaded store — now a worn attachment falls back to its live prim's `Properties.Name` and any still-nameless id is passed once to `Inventory.RequestFetchInventory` so the 2.5 s poll fills it in. 10 new tests. build (solution + `app/`) + 390 tests + `dotnet format` + `--selftest` 26/26 green. **Confirmed in-world 2026-08-30.** [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-UI-16-worn-items-tab.md) |
| BUG-UI-04 | Adjustable UI Window Transparency | ui | | ux-designer | | FEAT-UI-08 | Add a slider to Preferences (saved to `preferences.cfg`) to let users dynamically adjust the alpha opacity of the central Glassmorphism theme for better readability. [Spec](file:///E:/Git/SLNG/docs/specs/BUG-UI-04-reduce-window-transparency.md) |
| FEAT-UI-19 | Advanced Group Management | ui/net | | ux-designer | | M5-3 | Implement full group management: Group Profile (Insignia/Charter edit), Members & Roles (Abilities), Group Titles (switching active title), Notices, and Land/Tier contribution. [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-UI-19-advanced-group-management.md) |
| FEAT-INV-04 | Outfits browser — save / wear saved outfits ✅ | net/ui | claude | ux-designer | claude | FEAT-UI-16 | Firestorm-style **Outfits**: the `#Outfits` system folder, each subfolder a saved outfit. **Unblocked slice, `v0.11.15` → `v0.11.25-alpha`:** a third **Outfits** tab (Ctrl+O opens straight onto it) lists the `#Outfits` subfolders; each row is collapsible and lazy-loads its contents (type icons, gold for items also worn now), and the **currently-worn outfit** is marked ✅ (via the COF folder-link, falling back to "the fully-worn outfit with the most items" on OpenSim). Name field + **💾 Speichern** → `SaveCurrentOutfitAsync` (`CreateFolder` under `#Outfits`, reused on same name — no duplicate — + one `Inventory.CreateLinkAsync` per live worn item, names resolved first so no "Link" placeholders); the button doubles as **✏️ Umbenennen**. Right-click menu mirrors Firestorm: *Aktuelles Outfit ersetzen* (`ReplaceWornWithOutfitAttachmentsAsync` — detach what the outfit lacks, attach what's missing, write the COF folder-link), *Zu aktuellem Outfit hinzufügen* (`WearOutfitAttachmentsAsync`), *Von aktuellem Outfit entfernen* (`RemoveOutfitFromWornAsync`), *Outfit speichern (= akt. Getrage)* (`ReplaceOutfitWithCurrentAsync`), *Outfit neu benennen*, *Outfit löschen* (→ Trash). All avatar-facing actions are attachment-scoped; clothing/body swaps and a real Replace-Outfit are **FEAT-AVATAR-01 Phase 2** (the rebake path). New neutral surface: `MyOutfitsFolderId`, `GetSavedOutfitsAsync`, `GetOutfitContentsAsync`, `SaveCurrentOutfitAsync`, `AddCurrentToOutfitAsync`, `ReplaceOutfitWithCurrentAsync`, `WearOutfitAttachmentsAsync`, `ReplaceWornWithOutfitAttachmentsAsync`, `RemoveOutfitFromWornAsync`, `RenameOutfitAsync`, `DeleteOutfitAsync`; `OutfitEntry` in `SLNG.Core`. 8 new tests. build (solution + `app/`) + 396 tests + `dotnet format` + `--selftest` 26/26 green. **Confirmed in-world 2026-08-30.** [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-INV-04-outfits-browser.md) |
| FEAT-INV-03 | Outfit / COF write-back on detach + cleanup ✅ | net/ui | claude | protocol-re | claude | FEAT-UI-16 | Detaching in SLNG didn't survive a relog: `DetachItemAsync` only trashed a Current-Outfit link when nothing was attached, and `DetachAllAttachments` never touched the COF at all — so items stayed in the outfit and were re-worn on login, which is why the inventory kept showing them as "getragen". **Implemented `v0.11.11-alpha`, merged `v0.11.14-alpha`:** `DetachItemAsync` now trashes the COF link for a real attachment too; `DetachAllAttachments` reads each prim's `AttachItemID` and trashes those items' links after the `ObjectDetach` (shared `RemoveOutfitLinksForItems` helper). New `CleanUpCurrentOutfit()` → `OutfitCleanupResult` moves to Trash: dead links, links whose target is already in Trash, and links to `AssetType.Object` items not currently attached — **never** a Clothing/Bodypart link (needs a rebake, FEAT-AVATAR-01) or a worn item. "Is it worn" is read from the **scene** (prims parented to our avatar, by `AttachItemID`), not LibreMetaverse's `GetAttachmentsByItemId()` cache which lags a detach (`47f5f05`); a `[OutfitCleanup]` summary line logs what it kept/trashed and why. A **"🧹 Outfit aufräumen"** button in the Angezogen tab drives it; the Current Outfit folder is also re-fetched when the inventory window is shown, so a change made in another viewer shows without a relog. Everything moves links only, never real items — all recoverable from Trash. 3 new tests. build (solution + `app/`) + 390 tests + `dotnet format` + `--selftest` 26/26 green. **Confirmed in-world 2026-08-30.** [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-INV-03-outfit-cof-writeback.md) |
| FEAT-UI-20 | Outfit Gallery | ui | | ux-designer | | MVP3-1 | Visual grid view for saved outfits, allowing users to capture and assign thumbnail images to outfit folders for easier browsing. [Spec](file:///E:/Git/SLNG/docs/specs/FEAT-UI-20-outfit-gallery.md) |
